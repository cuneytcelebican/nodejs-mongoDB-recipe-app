<div class="row">
        <div class="col-sm-4 col-sm-offset-4">
            <div class="white-box registration-form">
                <div class="logo">
                    <a href="/"><img src="/img/logo.png"></a>
                </div>
                <div class="registration-form-items">
                    <form method="POST" action="/users/login">
                        {{#if success_msg}}
                        <div class="alert alert-success alert-dismissible" role="alert">
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                            {{success_msg}}
                        </div>
                        {{/if}}
                        {{#if error_msg}}
                        <div class="alert alert-warning alert-dismissible" role="alert">
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                            {{error_msg}}
                        </div>
                        {{/if}}
                        {{#if error}}
                        <div class="alert alert-warning alert-dismissible" role="alert">
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                            {{error}}
                        </div>
                        {{/if}}
                        <div class="form-group">
                            <label for="usernameField">Username</label>
                            <input type="text" class="form-control" name="username" placeholder="Username">
                        </div>
                        <div class="form-group">
                            <label for="passwordField">Password</label>
                            <input type="password" class="form-control" name="password" placeholder="Password">
                        </div>
                        
                        <button type="submit" class="btn btn-success">SUBMIT</button>

                        <div class="login-callback">Don't you have an account? <a href="/users/signin">Signin</a></div>
                    </form>
                </div>
            </div>
        </div>
    </div>
<div class="bg-hw-img"></div>
<!-- https://www.youtube.com/watch?v=PFP0oXNNveg -->